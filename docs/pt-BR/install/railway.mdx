---
read_when:
    - Implantar o OpenClaw no Railway
    - Você quer uma implantação em nuvem com um clique e Control UI no navegador
summary: Implante o OpenClaw no Railway com um template de um clique
title: Railway
x-i18n:
    generated_at: "2026-04-05T12:45:56Z"
    model: gpt-5.4
    provider: openai
    source_hash: f59699aa4e9d27d50018acc2e89ed1bbb1b1a5c85e3fd52f2afe5b2c97b21720
    source_path: install/railway.mdx
    workflow: 15
---

Implante o OpenClaw no Railway com um template de um clique e acesse-o pela Control UI web.
Este é o caminho mais fácil “sem terminal no servidor”: o Railway executa o Gateway para você.

## Checklist rápido (novos usuários)

1. Clique em **Deploy on Railway** (abaixo).
2. Adicione um **Volume** montado em `/data`.
3. Defina as **Variables** obrigatórias (pelo menos `OPENCLAW_GATEWAY_PORT` e `OPENCLAW_GATEWAY_TOKEN`).
4. Habilite **HTTP Proxy** na porta `8080`.
5. Abra `https://<your-railway-domain>/openclaw` e conecte-se usando o segredo compartilhado configurado. Este template usa `OPENCLAW_GATEWAY_TOKEN` por padrão; se você o substituir por autenticação por senha, use essa senha em vez disso.

## Implantação com um clique

<a href="https://railway.com/deploy/clawdbot-railway-template" target="_blank" rel="noreferrer">
  Deploy on Railway
</a>

Após a implantação, encontre sua URL pública em **Railway → seu serviço → Settings → Domains**.

O Railway irá:

- fornecer um domínio gerado (geralmente `https://<something>.up.railway.app`), ou
- usar seu domínio personalizado, se você tiver anexado um.

Depois, abra:

- `https://<your-railway-domain>/openclaw` — Control UI

## O que você recebe

- Gateway OpenClaw hospedado + Control UI
- Armazenamento persistente via Railway Volume (`/data`) para que `openclaw.json`,
  `auth-profiles.json` por agente, estado de canal/provedor, sessões e
  workspace sobrevivam a novas implantações

## Configurações obrigatórias do Railway

### Rede pública

Habilite **HTTP Proxy** para o serviço.

- Porta: `8080`

### Volume (obrigatório)

Anexe um volume montado em:

- `/data`

### Variáveis

Defina estas variáveis no serviço:

- `OPENCLAW_GATEWAY_PORT=8080` (obrigatório — deve corresponder à porta em Rede pública)
- `OPENCLAW_GATEWAY_TOKEN` (obrigatório; trate como um segredo de administrador)
- `OPENCLAW_STATE_DIR=/data/.openclaw` (recomendado)
- `OPENCLAW_WORKSPACE_DIR=/data/workspace` (recomendado)

## Conectar um canal

Use a Control UI em `/openclaw` ou execute `openclaw onboard` pelo shell do Railway para instruções de configuração de canais:

- [Telegram](/channels/telegram) (mais rápido — apenas um token de bot)
- [Discord](/channels/discord)
- [Todos os canais](/channels)

## Backups e migração

Exporte seu estado, configuração, perfis de autenticação e workspace:

```bash
openclaw backup create
```

Isso cria um arquivo de backup portátil com o estado do OpenClaw mais qualquer
workspace configurado. Consulte [Backup](/cli/backup) para detalhes.

## Próximos passos

- Configurar canais de mensagens: [Canais](/channels)
- Configurar o Gateway: [Configuração do Gateway](/gateway/configuration)
- Manter o OpenClaw atualizado: [Atualização](/install/updating)
