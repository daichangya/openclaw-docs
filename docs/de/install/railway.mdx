---
read_when:
    - OpenClaw auf Railway bereitstellen
    - Sie möchten eine One-Click-Cloud-Bereitstellung mit browserbasierter Control UI
summary: OpenClaw mit einer One-Click-Vorlage auf Railway bereitstellen
title: Railway
x-i18n:
    generated_at: "2026-04-05T12:47:32Z"
    model: gpt-5.4
    provider: openai
    source_hash: f59699aa4e9d27d50018acc2e89ed1bbb1b1a5c85e3fd52f2afe5b2c97b21720
    source_path: install/railway.mdx
    workflow: 15
---

OpenClaw mit einer One-Click-Vorlage auf Railway bereitstellen und über die webbasierte Control UI darauf zugreifen.
Dies ist der einfachste Weg „ohne Terminal auf dem Server“: Railway führt das Gateway für Sie aus.

## Kurze Checkliste (neue Benutzer)

1. Auf **Deploy on Railway** klicken (unten).
2. Ein **Volume** hinzufügen, das unter `/data` eingehängt wird.
3. Die erforderlichen **Variables** setzen (mindestens `OPENCLAW_GATEWAY_PORT` und `OPENCLAW_GATEWAY_TOKEN`).
4. **HTTP Proxy** auf Port `8080` aktivieren.
5. `https://<your-railway-domain>/openclaw` öffnen und sich mit dem konfigurierten Shared Secret verbinden. Diese Vorlage verwendet standardmäßig `OPENCLAW_GATEWAY_TOKEN`; wenn Sie es durch Passwortauthentifizierung ersetzen, verwenden Sie stattdessen dieses Passwort.

## One-Click-Bereitstellung

<a href="https://railway.com/deploy/clawdbot-railway-template" target="_blank" rel="noreferrer">
  Auf Railway bereitstellen
</a>

Nach der Bereitstellung finden Sie Ihre öffentliche URL unter **Railway → Ihr Service → Settings → Domains**.

Railway wird entweder:

- Ihnen eine generierte Domain geben (oft `https://<something>.up.railway.app`), oder
- Ihre benutzerdefinierte Domain verwenden, wenn Sie eine angehängt haben.

Dann öffnen Sie:

- `https://<your-railway-domain>/openclaw` — Control UI

## Was Sie erhalten

- Gehostetes OpenClaw Gateway + Control UI
- Persistenter Speicher über Railway Volume (`/data`), sodass `openclaw.json`,
  agent-spezifische `auth-profiles.json`, Kanal-/Provider-Zustand, Sitzungen und
  Workspace erneute Bereitstellungen überleben

## Erforderliche Railway-Einstellungen

### Öffentliches Netzwerk

Aktivieren Sie **HTTP Proxy** für den Service.

- Port: `8080`

### Volume (erforderlich)

Hängen Sie ein Volume ein unter:

- `/data`

### Variables

Setzen Sie diese Variablen für den Service:

- `OPENCLAW_GATEWAY_PORT=8080` (erforderlich — muss mit dem Port in Public Networking übereinstimmen)
- `OPENCLAW_GATEWAY_TOKEN` (erforderlich; als Admin-Secret behandeln)
- `OPENCLAW_STATE_DIR=/data/.openclaw` (empfohlen)
- `OPENCLAW_WORKSPACE_DIR=/data/workspace` (empfohlen)

## Einen Kanal verbinden

Verwenden Sie die Control UI unter `/openclaw` oder führen Sie `openclaw onboard` über die Railway-Shell aus, um Anweisungen zur Kanaleinrichtung zu erhalten:

- [Telegram](/channels/telegram) (am schnellsten — nur ein Bot-Token)
- [Discord](/channels/discord)
- [Alle Kanäle](/channels)

## Backups & Migration

Exportieren Sie Ihren Zustand, die Konfiguration, Auth-Profile und den Workspace:

```bash
openclaw backup create
```

Dadurch wird ein portables Backup-Archiv mit dem OpenClaw-Zustand plus jedem konfigurierten
Workspace erstellt. Siehe [Backup](/cli/backup) für Details.

## Nächste Schritte

- Messaging-Kanäle einrichten: [Channels](/channels)
- Das Gateway konfigurieren: [Gateway configuration](/gateway/configuration)
- OpenClaw aktuell halten: [Updating](/install/updating)
