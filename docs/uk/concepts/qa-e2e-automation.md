---
read_when:
    - Розширення qa-lab або qa-channel
    - Додавання QA-сценаріїв, що підтримуються репозиторієм
    - Побудова більш реалістичної QA-автоматизації навколо інформаційної панелі Gateway
summary: Форма приватної QA-автоматизації для qa-lab, qa-channel, початково заданих сценаріїв і звітів протоколу
title: QA E2E-автоматизація
x-i18n:
    generated_at: "2026-04-07T08:46:55Z"
    model: gpt-5.4
    provider: openai
    source_hash: e1e2c409438629f0ffcc06fcd1fce5203e92769be19d927db8fbf3fa995875af
    source_path: concepts/qa-e2e-automation.md
    workflow: 15
---

# QA E2E-автоматизація

Приватний стек QA призначений для тестування OpenClaw у більш реалістичний,
каналоподібний спосіб, ніж це може дати окремий модульний тест.

Поточні складові:

- `extensions/qa-channel`: синтетичний канал повідомлень із поверхнями DM, каналу, гілки,
  реакцій, редагування та видалення.
- `extensions/qa-lab`: UI для налагодження та QA-шина для спостереження за транскриптом,
  інʼєкції вхідних повідомлень і експорту Markdown-звіту.
- `qa/`: початкові ресурси з репозиторію для стартового завдання та базових QA
  сценаріїв.

Поточний робочий процес QA-оператора — це двопанельний QA-сайт:

- Ліворуч: інформаційна панель Gateway (Control UI) з агентом.
- Праворуч: QA Lab, що показує схожий на Slack транскрипт і план сценарію.

Запустіть це за допомогою:

```bash
pnpm qa:lab:up
```

Це збирає QA-сайт, запускає lane Gateway на базі Docker і відкриває
сторінку QA Lab, де оператор або цикл автоматизації може дати агенту QA-місію,
спостерігати реальну поведінку каналу та фіксувати, що спрацювало, що не
спрацювало або що залишилося заблокованим.

Для швидшої ітерації UI QA Lab без повторного збирання Docker-образу щоразу
запускайте стек із підключеним через bind mount бандлом QA Lab:

```bash
pnpm openclaw qa docker-build-image
pnpm qa:lab:build
pnpm qa:lab:up:fast
pnpm qa:lab:watch
```

`qa:lab:up:fast` зберігає сервіси Docker на попередньо зібраному образі та підключає через bind mount
`extensions/qa-lab/web/dist` у контейнер `qa-lab`. `qa:lab:watch`
перезбирає цей бандл після змін; оновлюйте браузер після кожного збирання.

## Початкові дані з репозиторію

Початкові ресурси містяться в `qa/`:

- `qa/QA_KICKOFF_TASK.md`
- `qa/seed-scenarios.json`

Вони навмисно зберігаються в git, щоб план QA був видимий і людям, і
агенту. Базовий список має залишатися достатньо широким, щоб охоплювати:

- DM і чат каналу
- поведінку гілок
- життєвий цикл дій із повідомленнями
- зворотні виклики cron
- відтворення памʼяті
- перемикання моделей
- передавання підзавдань субагенту
- читання репозиторію та документації
- одне невелике завдання зі збирання, наприклад Lobster Invaders

## Звітування

`qa-lab` експортує Markdown-звіт протоколу зі спостережуваної часової шкали шини.
Звіт має відповідати на запитання:

- Що спрацювало
- Що не спрацювало
- Що залишилося заблокованим
- Які сценарії подальших дій варто додати

## Пов’язана документація

- [Тестування](/uk/help/testing)
- [QA Channel](/uk/channels/qa-channel)
- [Інформаційна панель](/web/dashboard)
