---
read_when:
    - Пояснення використання токенів, витрат або вікон контексту
    - Налагодження зростання контексту або поведінки Compaction
summary: Як OpenClaw будує контекст промпту та звітує про використання токенів і витрати
title: Використання токенів і витрати
x-i18n:
    generated_at: "2026-04-25T19:29:24Z"
    model: gpt-5.4
    provider: openai
    source_hash: 828b282103902f55d65ce820c17753c2602169eff068bcea36e629759002f28d
    source_path: reference/token-use.md
    workflow: 15
---

# Використання токенів і витрати

OpenClaw відстежує **токени**, а не символи. Токени залежать від моделі, але більшість
моделей у стилі OpenAI у середньому мають ~4 символи на токен для англійського тексту.

## Як будується системний промпт

OpenClaw збирає власний системний промпт під час кожного запуску. Він містить:

- Список інструментів + короткі описи
- Список Skills (лише метадані; інструкції завантажуються за потреби через `read`).
  Компактний блок Skills обмежується `skills.limits.maxSkillsPromptChars`,
  з необов’язковим перевизначенням для окремого агента в
  `agents.list[].skillsLimits.maxSkillsPromptChars`.
- Інструкції для самостійного оновлення
- Робочий простір + bootstrap-файли (`AGENTS.md`, `SOUL.md`, `TOOLS.md`, `IDENTITY.md`, `USER.md`, `HEARTBEAT.md`, `BOOTSTRAP.md` для нових, а також `MEMORY.md`, якщо він є). `memory.md` у нижньому регістрі в корені не інжектується; це застарілий вхід для виправлення через `openclaw doctor --fix`, коли він використовується разом із `MEMORY.md`. Великі файли обрізаються через `agents.defaults.bootstrapMaxChars` (типово: 12000), а загальний обсяг bootstrap-інжекції обмежується `agents.defaults.bootstrapTotalMaxChars` (типово: 60000). Щоденні файли `memory/*.md` не входять до звичайного bootstrap-промпту; вони залишаються доступними за потреби через інструменти пам’яті під час звичайних ходів, але чисті `/new` і `/reset` можуть додавати одноразовий блок стартового контексту з нещодавньою щоденною пам’яттю для першого ходу. Цей стартовий вступ керується через `agents.defaults.startupContext`.
- Час (UTC + часовий пояс користувача)
- Теги відповіді + поведінка Heartbeat
- Метадані середовища виконання (host/OS/model/thinking)

Див. повний розбір у [System Prompt](/uk/concepts/system-prompt).

## Що входить до вікна контексту

Усе, що отримує модель, зараховується до ліміту контексту:

- Системний промпт (усі перелічені вище секції)
- Історія розмови (повідомлення користувача й асистента)
- Виклики інструментів і результати інструментів
- Вкладення/транскрипти (зображення, аудіо, файли)
- Підсумки Compaction і артефакти обрізання
- Обгортки провайдера або заголовки безпеки (не видимі, але все одно враховуються)

Деякі поверхні з великим навантаженням під час виконання мають власні явні обмеження:

- `agents.defaults.contextLimits.memoryGetMaxChars`
- `agents.defaults.contextLimits.memoryGetDefaultLines`
- `agents.defaults.contextLimits.toolResultMaxChars`
- `agents.defaults.contextLimits.postCompactionMaxChars`

Перевизначення для окремого агента розташовані в `agents.list[].contextLimits`. Ці параметри
призначені для обмежених за розміром фрагментів часу виконання та інжектованих блоків,
якими володіє runtime. Вони відокремлені від bootstrap-лімітів, лімітів
стартового контексту та лімітів промпту Skills.

Для зображень OpenClaw зменшує розмір зображень у транскриптах/інструментах перед викликами провайдера.
Використовуйте `agents.defaults.imageMaxDimensionPx` (типово: `1200`) для налаштування:

- Нижчі значення зазвичай зменшують використання vision-токенів і розмір payload.
- Вищі значення зберігають більше візуальних деталей для OCR або скриншотів із насиченим UI.

Для практичного розбору (для кожного інжектованого файла, інструментів, Skills і розміру системного промпту) використовуйте `/context list` або `/context detail`. Див. [Context](/uk/concepts/context).

## Як переглянути поточне використання токенів

Використовуйте це в чаті:

- `/status` → **картка статусу з емодзі**, яка показує модель сесії, використання контексту,
  токени введення/виведення останньої відповіді та **оцінену вартість** (лише для API key).
- `/usage off|tokens|full` → додає **підвал використання для кожної відповіді** до кожної відповіді.
  - Зберігається для сесії (записується як `responseUsage`).
  - OAuth-автентифікація **приховує вартість** (лише токени).
- `/usage cost` → показує локальний підсумок вартості з логів сесії OpenClaw.

Інші поверхні:

- **TUI/Web TUI:** підтримуються `/status` і `/usage`.
- **CLI:** `openclaw status --usage` і `openclaw channels list` показують
  нормалізовані вікна квот провайдера (`X% left`, а не витрати на кожну відповідь).
  Поточні провайдери для вікон використання: Anthropic, GitHub Copilot, Gemini CLI,
  OpenAI Codex, MiniMax, Xiaomi і z.ai.

Поверхні використання нормалізують поширені псевдоніми нативних полів провайдера перед показом.
Для трафіку OpenAI-family Responses це включає і `input_tokens` /
`output_tokens`, і `prompt_tokens` / `completion_tokens`, тож специфічні
для транспорту назви полів не змінюють `/status`, `/usage` або підсумки сесії.
Використання JSON у Gemini CLI також нормалізується: текст відповіді береться з `response`, а
`stats.cached` мапиться на `cacheRead`, при цьому використовується `stats.input_tokens - stats.cached`,
коли CLI не надає явного поля `stats.input`.
Для нативного трафіку OpenAI-family Responses псевдоніми використання у WebSocket/SSE
нормалізуються так само, а загальні значення повертаються до нормалізованих input + output, коли
`total_tokens` відсутній або дорівнює `0`.
Коли поточний знімок сесії містить мало даних, `/status` і `session_status` також можуть
відновлювати лічильники токенів/кешу та мітку активної runtime-моделі з
найновішого логу використання транскрипту. Наявні ненульові live-значення все ще мають
пріоритет над fallback-значеннями з транскрипту, а більші загальні значення
з транскрипту, орієнтовані на промпт, можуть перемагати, коли збережені загальні значення відсутні або менші.
Автентифікація використання для вікон квот провайдера надходить із гачків, специфічних для провайдера, коли це можливо;
інакше OpenClaw повертається до зіставлення облікових даних OAuth/API key
з auth profiles, змінних середовища або конфігурації.
Записи транскрипту асистента зберігають ту саму нормалізовану форму використання, включно з
`usage.cost`, коли для активної моделі налаштовано ціни і провайдер повертає метадані використання.
Це дає `/usage cost` і статусу сесії на основі транскрипту стабільне джерело навіть після того, як live-стан runtime зникне.

OpenClaw тримає облік використання провайдера окремо від поточного знімка контексту.
`usage.total` провайдера може включати кешоване введення, виведення та кілька викликів моделі
в циклі інструментів, тому це корисно для вартості й телеметрії, але може завищувати
live-вікно контексту. Відображення контексту та діагностика використовують найновіший знімок
промпту (`promptTokens`, або останній виклик моделі, якщо знімок промпту недоступний) для `context.used`.

## Оцінка вартості (коли показується)

Вартість оцінюється з конфігурації цін вашої моделі:

```
models.providers.<provider>.models[].cost
```

Це **USD за 1M токенів** для `input`, `output`, `cacheRead` і
`cacheWrite`. Якщо дані про ціну відсутні, OpenClaw показує лише токени. OAuth-токени
ніколи не показують вартість у доларах.

## Вплив TTL кешу та обрізання

Кешування промптів провайдера застосовується лише в межах вікна TTL кешу. OpenClaw може
необов’язково запускати **cache-ttl pruning**: він обрізає сесію після завершення TTL кешу,
а потім скидає вікно кешу, щоб наступні запити могли повторно використати
щойно кешований контекст замість повторного кешування всієї історії. Це допомагає
зменшити витрати на запис у кеш, коли сесія простоює довше за TTL.

Налаштуйте це в [Gateway configuration](/uk/gateway/configuration) і див. деталі
поведінки в [Session pruning](/uk/concepts/session-pruning).

Heartbeat може підтримувати кеш **теплим** під час періодів бездіяльності. Якщо TTL кешу
вашої моделі дорівнює `1h`, встановлення інтервалу heartbeat трохи меншим за це значення (наприклад, `55m`) може уникнути повторного кешування всього промпту, зменшуючи витрати на запис у кеш.

У конфігураціях із кількома агентами можна тримати одну спільну конфігурацію моделі та налаштовувати поведінку кешу
для кожного агента через `agents.list[].params.cacheRetention`.

Для повного покрокового керівництва за всіма параметрами див. [Prompt Caching](/uk/reference/prompt-caching).

Для цін Anthropic API читання з кешу значно дешевше за токени введення,
тоді як запис у кеш тарифікується за вищим множником. Актуальні ставки та множники TTL див. в документації Anthropic щодо цін на кешування промптів:
[https://docs.anthropic.com/docs/build-with-claude/prompt-caching](https://docs.anthropic.com/docs/build-with-claude/prompt-caching)

### Приклад: підтримувати кеш 1h теплим за допомогою heartbeat

```yaml
agents:
  defaults:
    model:
      primary: "anthropic/claude-opus-4-6"
    models:
      "anthropic/claude-opus-4-6":
        params:
          cacheRetention: "long"
    heartbeat:
      every: "55m"
```

### Приклад: змішаний трафік зі стратегією кешу для окремих агентів

```yaml
agents:
  defaults:
    model:
      primary: "anthropic/claude-opus-4-6"
    models:
      "anthropic/claude-opus-4-6":
        params:
          cacheRetention: "long" # типова базова поведінка для більшості агентів
  list:
    - id: "research"
      default: true
      heartbeat:
        every: "55m" # підтримувати довгий кеш теплим для глибоких сесій
    - id: "alerts"
      params:
        cacheRetention: "none" # уникати записів у кеш для імпульсних сповіщень
```

`agents.list[].params` зливається поверх `params` вибраної моделі, тож ви можете
перевизначити лише `cacheRetention` і успадкувати інші типові значення моделі без змін.

### Приклад: увімкнути beta-заголовок Anthropic 1M context

Вікно контексту Anthropic 1M наразі доступне лише через beta. OpenClaw може інжектувати
потрібне значення `anthropic-beta`, коли ви вмикаєте `context1m` на підтримуваних моделях Opus
або Sonnet.

```yaml
agents:
  defaults:
    models:
      "anthropic/claude-opus-4-6":
        params:
          context1m: true
```

Це мапиться на beta-заголовок Anthropic `context-1m-2025-08-07`.

Це застосовується лише тоді, коли `context1m: true` встановлено для цього запису моделі.

Вимога: облікові дані мають бути придатними для використання довгого контексту. Якщо ні,
Anthropic відповідає помилкою обмеження швидкості на боці провайдера для цього запиту.

Якщо ви автентифікуєте Anthropic за допомогою OAuth/subscription-токенів (`sk-ant-oat-*`),
OpenClaw пропускає beta-заголовок `context-1m-*`, оскільки Anthropic наразі
відхиляє таку комбінацію з HTTP 401.

## Поради для зменшення тиску на токени

- Використовуйте `/compact`, щоб підсумовувати довгі сесії.
- Обрізайте великі результати інструментів у своїх робочих процесах.
- Зменшуйте `agents.defaults.imageMaxDimensionPx` для сесій із великою кількістю скриншотів.
- Тримайте описи Skills короткими (список Skills інжектується в промпт).
- Для багатослівної дослідницької роботи віддавайте перевагу меншим моделям.

Див. [Skills](/uk/tools/skills) для точної формули накладних витрат списку Skills.

## Пов’язане

- [API usage and costs](/uk/reference/api-usage-costs)
- [Prompt caching](/uk/reference/prompt-caching)
- [Usage tracking](/uk/concepts/usage-tracking)
