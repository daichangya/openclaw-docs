---
read_when:
    - Зміна рендерингу виводу асистента в Control UI
    - Налагодження директив представлення `[embed ...]`, `MEDIA:`, reply або audio
summary: Протокол shortcode для розширеного виводу для embed-ів, медіа, підказок аудіо та відповідей
title: Протокол розширеного виводу
x-i18n:
    generated_at: "2026-04-23T06:47:24Z"
    model: gpt-5.4
    provider: openai
    source_hash: 566338ac0571c6ab9062c6bad0bc4f71fe65249a3fcd9d8e575affcd93db11e7
    source_path: reference/rich-output-protocol.md
    workflow: 15
---

# Протокол розширеного виводу

Вивід асистента може містити невеликий набір директив доставки/рендерингу:

- `MEDIA:` для доставки вкладень
- `[[audio_as_voice]]` для підказок представлення аудіо
- `[[reply_to_current]]` / `[[reply_to:<id>]]` для метаданих відповіді
- `[embed ...]` для розширеного рендерингу в Control UI

Ці директиви є окремими. `MEDIA:` і теги reply/voice залишаються метаданими доставки; `[embed ...]` — це вебшлях лише для розширеного рендерингу.

## `[embed ...]`

`[embed ...]` — це єдиний агент-орієнтований синтаксис розширеного рендерингу для Control UI.

Приклад самозакривного запису:

```text
[embed ref="cv_123" title="Status" /]
```

Правила:

- `[view ...]` більше не є валідним для нового виводу.
- Shortcode-и embed рендеряться лише на поверхні повідомлення асистента.
- Рендеряться лише embed-и з URL-підкладкою. Використовуйте `ref="..."` або `url="..."`.
- Shortcode-и embed у block-формі з вбудованим HTML не рендеряться.
- Веб-UI прибирає shortcode із видимого тексту та рендерить embed вбудовано.
- `MEDIA:` не є псевдонімом embed і не повинен використовуватися для розширеного рендерингу embed.

## Форма збереженого рендерингу

Нормалізований/збережений блок вмісту асистента — це структурований елемент `canvas`:

```json
{
  "type": "canvas",
  "preview": {
    "kind": "canvas",
    "surface": "assistant_message",
    "render": "url",
    "viewId": "cv_123",
    "url": "/__openclaw__/canvas/documents/cv_123/index.html",
    "title": "Status",
    "preferredHeight": 320
  }
}
```

Збережені/відрендерені розширені блоки безпосередньо використовують цю форму `canvas`. `present_view` не розпізнається.
