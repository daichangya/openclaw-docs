---
read_when:
    - Зміна виводу або форматів журналювання
    - Налагодження виводу CLI або Gateway
summary: Поверхні журналювання, журнали у файлах, стилі журналів WS та форматування консолі
title: журналювання Gateway
x-i18n:
    generated_at: "2026-04-26T19:17:01Z"
    model: gpt-5.4
    provider: openai
    source_hash: 52b3904ee693bdabc74b56e6dd924f12215ce741496a03eafc15f74258ad036e
    source_path: gateway/logging.md
    workflow: 15
---

# Журналювання

Огляд для користувачів (CLI + Control UI + конфігурація) див. у [/logging](/uk/logging).

OpenClaw має дві «поверхні» журналювання:

- **Вивід у консоль** (те, що ви бачите в терміналі / Debug UI).
- **Файлові журнали** (рядки JSON), які записує реєстратор Gateway.

## Файловий реєстратор

- Типовий файл журналу з ротацією розміщується в `/tmp/openclaw/` (один файл на день): `openclaw-YYYY-MM-DD.log`
  - Для дати використовується локальний часовий пояс хоста Gateway.
- Активні файли журналу ротуються на `logging.maxFileBytes` (типово: 100 MB), із збереженням
  до п’яти нумерованих архівів і продовженням запису в новий активний файл.
- Шлях до файлу журналу та рівень можна налаштувати через `~/.openclaw/openclaw.json`:
  - `logging.file`
  - `logging.level`

Формат файлу — один об’єкт JSON на рядок.

Вкладка Logs у Control UI відстежує цей файл через Gateway (`logs.tail`).
CLI може робити те саме:

```bash
openclaw logs --follow
```

**Докладний режим і рівні журналювання**

- **Файлові журнали** керуються виключно через `logging.level`.
- `--verbose` впливає лише на **докладність виводу в консоль** (і стиль журналів WS); він **не**
  підвищує рівень файлових журналів.
- Щоб зберігати деталі, доступні лише в докладному режимі, у файлових журналах, установіть `logging.level` на `debug` або
  `trace`.

## Перехоплення консолі

CLI перехоплює `console.log/info/warn/error/debug/trace` і записує їх у файлові журнали,
продовжуючи водночас виводити їх у stdout/stderr.

Ви можете окремо налаштувати докладність консолі через:

- `logging.consoleLevel` (типово `info`)
- `logging.consoleStyle` (`pretty` | `compact` | `json`)

## Редагування чутливих даних

OpenClaw може маскувати чутливі токени до того, як вивід журналу або стенограми
покине процес. Та сама політика редагування застосовується до консолі, файлового журналу, записів журналу OTLP
та текстових приймачів стенограм сеансу, тому відповідні секретні значення маскуються до того, як рядки JSONL або повідомлення буде записано на диск.

- `logging.redactSensitive`: `off` | `tools` (типово: `tools`)
- `logging.redactPatterns`: масив рядків regex (перевизначає типові значення)
  - Використовуйте необроблені рядки regex (автоматично `gi`) або `/pattern/flags`, якщо потрібні власні прапорці.
  - Збіги маскуються зі збереженням перших 6 + останніх 4 символів (довжина >= 18), інакше `***`.
  - Типові значення охоплюють поширені присвоєння ключів, прапорці CLI, поля JSON, заголовки bearer, блоки PEM та популярні префікси токенів.

## Журнали WebSocket Gateway

Gateway виводить журнали протоколу WebSocket у двох режимах:

- **Звичайний режим (без `--verbose`)**: виводяться лише «цікаві» результати RPC:
  - помилки (`ok=false`)
  - повільні виклики (типовий поріг: `>= 50ms`)
  - помилки розбору
- **Докладний режим (`--verbose`)**: виводиться весь трафік запитів/відповідей WS.

### Стиль журналів WS

`openclaw gateway` підтримує перемикач стилю для кожного Gateway:

- `--ws-log auto` (типово): звичайний режим оптимізований; докладний режим використовує компактний вивід
- `--ws-log compact`: компактний вивід (об’єднані пари запит/відповідь) у докладному режимі
- `--ws-log full`: повний вивід по кожному кадру в докладному режимі
- `--compact`: псевдонім для `--ws-log compact`

Приклади:

```bash
# optimized (only errors/slow)
openclaw gateway

# show all WS traffic (paired)
openclaw gateway --verbose --ws-log compact

# show all WS traffic (full meta)
openclaw gateway --verbose --ws-log full
```

## Форматування консолі (журналювання підсистем)

Форматувач консолі **враховує TTY** і виводить узгоджені рядки з префіксами.
Реєстратори підсистем зберігають вивід згрупованим і зручним для перегляду.

Поведінка:

- **Префікси підсистем** у кожному рядку (наприклад, `[gateway]`, `[canvas]`, `[tailscale]`)
- **Кольори підсистем** (сталі для кожної підсистеми) плюс кольори рівнів
- **Колір, коли вивід є TTY або середовище схоже на повноцінний термінал** (`TERM`/`COLORTERM`/`TERM_PROGRAM`), з урахуванням `NO_COLOR`
- **Скорочені префікси підсистем**: відкидає початкові `gateway/` + `channels/`, зберігає останні 2 сегменти (наприклад, `whatsapp/outbound`)
- **Підреєстратори за підсистемами** (автоматичний префікс + структуроване поле `{ subsystem }`)
- **`logRaw()`** для виводу QR/UX (без префікса, без форматування)
- **Стилі консолі** (наприклад, `pretty | compact | json`)
- **Рівень журналу консолі** окремо від рівня файлового журналу (файл зберігає повну деталізацію, коли `logging.level` встановлено на `debug`/`trace`)
- **Тіла повідомлень WhatsApp** журналюються на рівні `debug` (використовуйте `--verbose`, щоб їх бачити)

Це дає змогу зберегти стабільність наявних файлових журналів, водночас роблячи інтерактивний вивід зручним для перегляду.

## Пов’язано

- [Журналювання](/uk/logging)
- [Експорт OpenTelemetry](/uk/gateway/opentelemetry)
- [Експорт діагностики](/uk/gateway/diagnostics)
