---
read_when:
    - Налаштування UI mac menu або логіки стану
summary: Логіка стану в рядку меню і що показується користувачам
title: Рядок меню
x-i18n:
    generated_at: "2026-04-24T04:16:52Z"
    model: gpt-5.4
    provider: openai
    source_hash: 89b03f3b0f9e56057d4cbf10bd1252372c65a2b2ae5e0405a844e9a59b51405d
    source_path: platforms/mac/menu-bar.md
    workflow: 15
---

# Логіка стану в рядку меню

## Що показується

- У значку рядка меню та в першому рядку стану меню ми показуємо поточний стан роботи агента.
- Поки робота активна, стан справності приховується; він повертається, коли всі сесії простоюють.
- Блок “Nodes” у меню перелічує лише **пристрої** (спарені вузли через `node.list`), а не записи client/presence.
- Розділ “Usage” з’являється під Context, коли доступні snapshot використання провайдера.

## Модель стану

- Сесії: події надходять із `runId` (для окремого запуску) плюс `sessionKey` у payload. “Main” сесія — це ключ `main`; якщо його немає, ми використовуємо найнещодавніше оновлену сесію.
- Пріоритет: main завжди перемагає. Якщо main активна, її стан показується негайно. Якщо main простоює, показується найнещодавніше активна не-main сесія. Ми не перемикаємося туди-сюди посеред активності; перемикання відбувається лише тоді, коли поточна сесія переходить у простій або main стає активною.
- Види активності:
  - `job`: виконання високорівневої команди (`state: started|streaming|done|error`).
  - `tool`: `phase: start|result` з `toolName` і `meta/args`.

## Перелік `IconState` (Swift)

- `idle`
- `workingMain(ActivityKind)`
- `workingOther(ActivityKind)`
- `overridden(ActivityKind)` (debug override)

### `ActivityKind` → гліф

- `exec` → 💻
- `read` → 📄
- `write` → ✍️
- `edit` → 📝
- `attach` → 📎
- типове значення → 🛠️

### Візуальне зіставлення

- `idle`: звичайний critter.
- `workingMain`: badge з гліфом, повне тонування, анімація руху лапок “working”.
- `workingOther`: badge з гліфом, приглушене тонування, без метушні.
- `overridden`: використовує вибраний гліф/тонування незалежно від активності.

## Текст рядка стану (меню)

- Поки робота активна: `<Session role> · <activity label>`
  - Приклади: `Main · exec: pnpm test`, `Other · read: apps/macos/Sources/OpenClaw/AppState.swift`.
- У стані простою: повертається до зведення справності.

## Обробка подій

- Джерело: події `agent` каналу control (`ControlChannel.handleAgentEvent`).
- Розібрані поля:
  - `stream: "job"` з `data.state` для запуску/зупинки.
  - `stream: "tool"` з `data.phase`, `name`, необов’язковими `meta`/`args`.
- Мітки:
  - `exec`: перший рядок `args.command`.
  - `read`/`write`: скорочений шлях.
  - `edit`: шлях плюс виведений тип зміни з `meta`/кількості змін у diff.
  - резервний варіант: назва інструмента.

## Debug override

- Settings ▸ Debug ▸ вибір “Icon override”:
  - `System (auto)` (типово)
  - `Working: main` (для кожного типу інструмента)
  - `Working: other` (для кожного типу інструмента)
  - `Idle`
- Зберігається через `@AppStorage("iconOverride")`; зіставляється з `IconState.overridden`.

## Контрольний список тестування

- Запустіть job main-сесії: перевірте, що значок перемикається одразу, а рядок стану показує мітку main.
- Запустіть job не-main сесії, поки main простоює: значок/стан показує не-main; залишається стабільним, доки завдання не завершиться.
- Запустіть main, поки active інша сесія: значок миттєво перемикається на main.
- Швидкі сплески інструментів: переконайтеся, що badge не мерехтить (TTL grace на результати інструментів).
- Рядок справності з’являється знову, коли всі сесії переходять у простій.

## Пов’язане

- [Застосунок macOS](/uk/platforms/macos)
- [Значок рядка меню](/uk/platforms/mac/icon)
