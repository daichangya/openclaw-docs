---
read_when:
    - Ви хочете зрозуміти, як Потік завдань пов’язаний із фоновими завданнями
    - Ви зустрічаєте Task Flow або потік завдань openclaw у примітках до випуску чи документації
    - Ви хочете перевірити або керувати стійким станом потоку
summary: Рівень оркестрації потоків Task Flow над фоновими завданнями
title: Потік завдань
x-i18n:
    generated_at: "2026-04-23T05:40:58Z"
    model: gpt-5.4
    provider: openai
    source_hash: f94a3cda89db5bfcc6c396358bc3fcee40f9313e102dc697d985f40707381468
    source_path: automation/taskflow.md
    workflow: 15
---

# Потік завдань

Потік завдань — це основа оркестрації потоків, що розташована над [фоновими завданнями](/uk/automation/tasks). Він керує стійкими багатокроковими потоками з власним станом, відстеженням ревізій і семантикою синхронізації, тоді як окремі завдання залишаються одиницею відокремленої роботи.

## Коли використовувати Потік завдань

Використовуйте Потік завдань, коли робота охоплює кілька послідовних або розгалужених кроків і вам потрібне стійке відстеження прогресу між перезапусками Gateway. Для окремих фонових операцій достатньо звичайного [завдання](/uk/automation/tasks).

| Сценарій                              | Використання              |
| ------------------------------------- | ------------------------- |
| Одне фонове завдання                  | Звичайне завдання         |
| Багатокроковий конвеєр (A, потім B, потім C) | Потік завдань (керований) |
| Спостереження за завданнями, створеними ззовні | Потік завдань (дзеркальний) |
| Одноразове нагадування                | завдання Cron             |

## Режими синхронізації

### Керований режим

Потік завдань повністю керує життєвим циклом. Він створює завдання як кроки потоку, доводить їх до завершення та автоматично просуває стан потоку.

Приклад: потік щотижневого звіту, який (1) збирає дані, (2) генерує звіт і (3) доставляє його. Потік завдань створює кожен крок як фонове завдання, очікує завершення, а потім переходить до наступного кроку.

```text
Flow: weekly-report
  Step 1: gather-data     → task created → succeeded
  Step 2: generate-report → task created → succeeded
  Step 3: deliver         → task created → running
```

### Дзеркальний режим

Потік завдань спостерігає за завданнями, створеними ззовні, і підтримує стан потоку синхронізованим, не перебираючи на себе відповідальність за створення завдань. Це корисно, коли завдання походять із завдань Cron, команд CLI або інших джерел, а ви хочете мати єдине уявлення про їхній прогрес як про потік.

Приклад: три незалежні завдання Cron, які разом утворюють ранкову операційну процедуру. Дзеркальний потік відстежує їхній сукупний прогрес, не керуючи тим, коли і як вони виконуються.

## Стійкий стан і відстеження ревізій

Кожен потік зберігає власний стан і відстежує ревізії, тому прогрес зберігається після перезапусків Gateway. Відстеження ревізій дає змогу виявляти конфлікти, коли кілька джерел одночасно намагаються просунути той самий потік.

## Поведінка скасування

`openclaw tasks flow cancel` встановлює для потоку стійкий намір скасування. Активні завдання в межах потоку скасовуються, і нові кроки не запускаються. Намір скасування зберігається між перезапусками, тому скасований потік залишається скасованим, навіть якщо Gateway перезапуститься до завершення всіх дочірніх завдань.

## Команди CLI

```bash
# List active and recent flows
openclaw tasks flow list

# Show details for a specific flow
openclaw tasks flow show <lookup>

# Cancel a running flow and its active tasks
openclaw tasks flow cancel <lookup>
```

| Команда                           | Опис                                          |
| --------------------------------- | --------------------------------------------- |
| `openclaw tasks flow list`        | Показує відстежувані потоки зі статусом і режимом синхронізації |
| `openclaw tasks flow show <id>`   | Переглянути один потік за ідентифікатором потоку або ключем пошуку |
| `openclaw tasks flow cancel <id>` | Скасувати запущений потік і його активні завдання |

## Як потоки пов’язані із завданнями

Потоки координують завдання, а не замінюють їх. Один потік може керувати кількома фоновими завданнями протягом свого життєвого циклу. Використовуйте `openclaw tasks`, щоб переглядати окремі записи завдань, і `openclaw tasks flow`, щоб переглядати потік оркестрації.

## Пов’язане

- [Фонові завдання](/uk/automation/tasks) — реєстр відокремленої роботи, яку координують потоки
- [CLI: tasks](/cli/tasks) — довідник команд CLI для `openclaw tasks flow`
- [Огляд автоматизації](/uk/automation) — усі механізми автоматизації з одного погляду
- [Завдання Cron](/uk/automation/cron-jobs) — заплановані завдання, які можуть слугувати джерелом для потоків
