---
read_when:
    - Вам потрібні цільові журнали налагодження без підвищення глобальних рівнів журналювання
    - Вам потрібно зафіксувати журнали, специфічні для підсистеми, для підтримки
summary: Прапорці діагностики для цільових журналів налагодження
title: Прапорці діагностики
x-i18n:
    generated_at: "2026-04-24T04:13:32Z"
    model: gpt-5.4
    provider: openai
    source_hash: b7e5ec9c5e28ef51f1e617baf62412897df8096f227a74d86a0824e269aafd9d
    source_path: diagnostics/flags.md
    workflow: 15
---

Прапорці діагностики дають змогу вмикати цільові журнали налагодження без увімкнення докладного журналювання всюди. Прапорці працюють за принципом opt-in і не мають ефекту, якщо підсистема їх не перевіряє.

## Як це працює

- Прапорці — це рядки (без урахування регістру).
- Ви можете вмикати прапорці в конфігурації або через перевизначення env.
- Підтримуються шаблони:
  - `telegram.*` відповідає `telegram.http`
  - `*` вмикає всі прапорці

## Увімкнення через конфігурацію

```json
{
  "diagnostics": {
    "flags": ["telegram.http"]
  }
}
```

Кілька прапорців:

```json
{
  "diagnostics": {
    "flags": ["telegram.http", "gateway.*"]
  }
}
```

Після зміни прапорців перезапустіть Gateway.

## Перевизначення через env (разово)

```bash
OPENCLAW_DIAGNOSTICS=telegram.http,telegram.payload
```

Вимкнути всі прапорці:

```bash
OPENCLAW_DIAGNOSTICS=0
```

## Куди потрапляють журнали

Прапорці надсилають журнали до стандартного файла журналу діагностики. Типово:

```
/tmp/openclaw/openclaw-YYYY-MM-DD.log
```

Якщо ви встановили `logging.file`, використовуйте натомість цей шлях. Журнали мають формат JSONL (один JSON-об’єкт на рядок). Редагування чутливих даних усе одно застосовується на основі `logging.redactSensitive`.

## Витяг журналів

Виберіть найновіший файл журналу:

```bash
ls -t /tmp/openclaw/openclaw-*.log | head -n 1
```

Відфільтруйте діагностику Telegram HTTP:

```bash
rg "telegram http error" /tmp/openclaw/openclaw-*.log
```

Або виконайте tail під час відтворення:

```bash
tail -f /tmp/openclaw/openclaw-$(date +%F).log | rg "telegram http error"
```

Для віддалених Gateway ви також можете використовувати `openclaw logs --follow` (див. [/cli/logs](/uk/cli/logs)).

## Примітки

- Якщо `logging.level` встановлено вище за `warn`, ці журнали можуть бути приглушені. Типове значення `info` підходить.
- Прапорці безпечно залишати увімкненими; вони впливають лише на обсяг журналів для конкретної підсистеми.
- Використовуйте [/logging](/uk/logging), щоб змінювати місця призначення журналів, рівні та редагування чутливих даних.

## Пов’язане

- [Діагностика Gateway](/uk/gateway/diagnostics)
- [Усунення проблем Gateway](/uk/gateway/troubleshooting)
