---
read_when:
    - Зміна поведінки або значень за замовчуванням для слів голосової активації
    - Додавання нових платформ Node, яким потрібна синхронізація слів активації
summary: Глобальні слова активації голосу (належать Gateway) і як вони синхронізуються між вузлами
title: Голосова активація
x-i18n:
    generated_at: "2026-04-23T23:01:23Z"
    model: gpt-5.4
    provider: openai
    source_hash: 5094c17aaa7f868beb81d04f7dc60565ded1852cc5c835a33de64dbd3da74bb4
    source_path: nodes/voicewake.md
    workflow: 15
---

OpenClaw розглядає **слова голосової активації як один глобальний список**, яким володіє **Gateway**.

- **Немає окремих користувацьких слів активації для кожного Node**.
- **Будь-який UI node/застосунку може редагувати** цей список; зміни зберігаються Gateway і транслюються всім.
- macOS та iOS зберігають локальні перемикачі **увімкнення/вимкнення голосової активації** (локальний UX і дозволи відрізняються).
- На Android голосова активація зараз вимкнена і використовується ручний потік мікрофона у вкладці Voice.

## Зберігання (хост Gateway)

Слова активації зберігаються на машині gateway за адресою:

- `~/.openclaw/settings/voicewake.json`

Структура:

```json
{ "triggers": ["openclaw", "claude", "computer"], "updatedAtMs": 1730000000000 }
```

## Протокол

### Методи

- `voicewake.get` → `{ triggers: string[] }`
- `voicewake.set` з параметрами `{ triggers: string[] }` → `{ triggers: string[] }`

Примітки:

- Тригери нормалізуються (обрізаються пробіли, порожні значення відкидаються). Порожні списки повертаються до значень за замовчуванням.
- Для безпеки застосовуються обмеження (ліміти кількості/довжини).

### Події

- payload `voicewake.changed` `{ triggers: string[] }`

Хто її отримує:

- Усі клієнти WebSocket (застосунок macOS, WebChat тощо)
- Усі підключені Node (iOS/Android), а також під час підключення Node як початкове надсилання “поточного стану”.

## Поведінка клієнтів

### Застосунок macOS

- Використовує глобальний список для фільтрації тригерів `VoiceWakeRuntime`.
- Редагування “Trigger words” у налаштуваннях Voice Wake викликає `voicewake.set`, а потім покладається на трансляцію для синхронізації з іншими клієнтами.

### Node iOS

- Використовує глобальний список для виявлення тригерів `VoiceWakeManager`.
- Редагування Wake Words у Settings викликає `voicewake.set` (через WS Gateway) і також зберігає локальне виявлення слів активації чутливим до змін.

### Node Android

- Голосова активація зараз вимкнена в середовищі виконання/Settings Android.
- Голос на Android використовує ручне захоплення мікрофона у вкладці Voice замість тригерів слів активації.

## Пов’язано

- [Режим Talk](/uk/nodes/talk)
- [Аудіо та голосові нотатки](/uk/nodes/audio)
- [Розуміння медіа](/uk/nodes/media-understanding)
